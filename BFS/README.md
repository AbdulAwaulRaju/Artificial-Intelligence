# Breadth First Search (BFS)

BFS is a graph traversal algorithm that explores nodes level by level.

## Idea
- Uses queue (FIFO)
- Visits neighbors first
- Then goes deeper

## Time Complexity
O(V + E)

## Space Complexity
O(V)
